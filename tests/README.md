# Tests

These solutions are written in LeetCode-compatible style. For local tests, create small driver files that instantiate `Solution` or the design class and compare outputs with the official examples.

Because LeetCode uses hidden harness classes such as `ListNode`, `TreeNode`, `Node`, some linked-list/tree/design problems need helper builders before they can be run locally.
